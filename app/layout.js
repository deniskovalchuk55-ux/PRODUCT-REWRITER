export const metadata = {
  title: "Упаковка товару",
  description: "AI-рерайт описів товарів",
};

export default function RootLayout({ children }) {
  return (
    <html lang="uk">
      <body style={{ margin: 0, background: "#f8fafc" }}>{children}</body>
    </html>
  );
}
